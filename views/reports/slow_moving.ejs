<%- include('../partials/header') %>
<%- include('../partials/sidebar') %>

<div id="content-wrapper">
  <%- include('../partials/navbar') %>

  <div class="container-fluid p-4 fade-in-up">
    <div class="d-flex align-items-center justify-content-between mb-4 flex-wrap gap-3">
      <h4 class="fw-bold mb-0"><i class="bi bi-tortoise me-2 text-secondary"></i>Slow Moving Products Report</h4>
      <a href="/reports" class="btn btn-outline-secondary btn-sm"><i class="bi bi-arrow-left me-1"></i>Back to Hub</a>
    </div>

    <!-- Filter (Super Admin only) -->
    <% if (role === 'Super Admin') { %>
      <div class="erp-card mb-4">
        <form action="/reports/slow-moving" method="GET" class="row g-3 align-items-end">
          <div class="col-md-8">
            <label for="branchId" class="form-label fw-semibold small text-muted text-uppercase">Filter by Branch</label>
            <select class="form-select" id="branchId" name="branchId">
              <option value="">All Branches</option>
              <% branches.forEach(b => { %>
                <option value="<%= b._id %>" <%= selectedBranch === b._id.toString() ? 'selected' : '' %>><%= b.branch_name %> (<%= b.city %>)</option>
              <% }) %>
            </select>
          </div>
          <div class="col-md-4">
            <button type="submit" class="btn btn-primary w-100 py-2"><i class="bi bi-funnel-fill me-1"></i>Generate Report</button>
          </div>
        </form>
      </div>
    <% } %>

    <!-- Report Table -->
    <div class="erp-card">
      <div class="table-responsive">
        <table class="table table-hover align-middle datatable">
          <thead>
            <tr>
              <th>Barcode</th>
              <th>Item Name</th>
              <th>Design No</th>
              <th>Size</th>
              <th>Colour</th>
              <th>Branch</th>
              <th class="text-end">Current Stock Qty</th>
              <th class="text-end text-danger">Units Sold (30 Days)</th>
              <th class="text-end">Rate</th>
              <th class="text-end">Stock Asset Value</th>
            </tr>
          </thead>
          <tbody>
            <% data.forEach(row => { %>
              <tr>
                <td class="font-monospace small"><%= row.stock.product_id.barcode || '-' %></td>
                <td class="fw-bold"><%= row.stock.product_id.item_name %></td>
                <td><span class="badge bg-secondary bg-opacity-25 text-dark font-monospace"><%= row.stock.product_id.design_no %></span></td>
                <td class="fw-semibold"><%= row.stock.product_id.size %></td>
                <td><%= row.stock.product_id.colour || '-' %></td>
                <td><span class="small fw-semibold text-secondary"><%= row.stock.branch_id.branch_name %></span></td>
                <td class="text-end fw-semibold text-dark"><%= row.stock.quantity %></td>
                <td class="text-end fw-bold text-danger"><%= row.totalSold %></td>
                <td class="text-end">₹<%= row.stock.product_id.rate.toFixed(2) %></td>
                <td class="text-end fw-semibold text-success">
                  ₹<%= (row.stock.quantity * row.stock.product_id.rate).toLocaleString('en-IN') %>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>

  </div>
</div>

<%- include('../partials/footer') %>
